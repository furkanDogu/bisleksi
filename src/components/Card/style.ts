import { ScaledSheet } from 'react-native-size-matters';

export default ScaledSheet.create({
    container: {
        width: '255.3@s',
        height: '298.4@vs',
        marginRight: '12.8@s',
        justifyContent: 'center',
        borderRadius: 15,
        alignItems: 'center',
    },
    textStyle: { fontSize: '21@s', color: 'white', fontWeight: 'bold', marginBottom: '15@vs' },
});
